#include <bits/stdc++.h>
using namespace std;

int main(){

    int ts;
    long long int A, B;
    string a, b, test = "0000";

    cin >> ts;

    for(int i = 0; i < ts; i++){
        cin >> a >> b;
        A = 0;
        B = 0;

        for(int j = 0; j < a.length(); j++){
            A += a[j] - '0';
        }

        for(int j = 0; j < b.length(); j++){
            B += b[j] - '0';
        }

        for(int j = 0; test != b)
    }


}
