#include <bits/stdc++.h>
#define ll long long
#define fori(n) for(ll i = 0; i < n; i++)
#define forj(n) for(ll j = 0; j < n; j++)
#define print(str) cout << str
#define nl cout << endl;
using namespace std;

int main(){

    string str;
    int j, flag;
    cin >> str;

    fori(str.length()){
        j = i+1;
        flag = 1;
        while(j < str.length()){
            if(str[i] == str[j]){
                //flag = 1;
                break;
            }
        }

        if(flag == 1){
            cout <<
            break;
        }
    }
    
}